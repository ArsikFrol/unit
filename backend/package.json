{
    "scripts": {
        "dev": "next dev -p 3001",
        "db:all": "npx prisma migrate reset --force && npx prisma migrate dev --name rename && npx prisma generate && npx prisma db seed",
        "db:seed": "npx prisma migrate reset --force && npx prisma db seed"
    },
    "dependencies": {
        "@prisma/adapter-pg": "7.8.0",
        "@prisma/client": "7.8.0",
        "next": "^16.3.5",
        "pg": "^8.23.0",
        "react": "^19.3.0",
        "react-dom": "^19.3.0"
    },
    "devDependencies": {
        "prisma": "7.8.0",
        "tsx": "^4.23.15",
        "typescript": "7.0.2"
    }
}
